<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All properties</title>
    <link rel="stylesheet" href="/listing.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css">
</head>
<body>
    <!-- <% for(let property of properties) { %> 
    <div>
        <p>Title : <%= property.title %> </p>
         <img src="<%= property.image.url %>" height="300px" width="300px" alt="Random image">
          <p>Description : <%= property.description %> </p>
          <p><%= property.price%> Rupee per night  </p>
           <p><%= property.location%></p>
           <p><%= property.country%></p> 
    </div>
    <button onclick="window.location.href = 'http://localhost:8080/listing/<%= property._id%>'">See in detail</button>
<%} %>
 <button onclick="window.location.href = 'http://localhost:8080/listing/add'">Add a new place</button> -->

 <div class="header">

    <div class="">
        <i class="fa-brands fa-safari logo"></i>
    </div>
    <div class="home text ">Home</div>
    <div class="all-listing text ">All Listings</div>
    <div class="add-place text ">Airbnb your Home </div>
 </div>

 <h1 align = "center">All Content</h1>
 <br>
 <div class="content">
          <% let i = 0%>
        <% while(i < properties.length) { %> 
            <div class="row">
           <% let j = i; %> 
           <%  while(  i < properties.length && i < j + 3){ %> 
                <div class="col">
                    <img src="<%= properties[i].image.url%>" alt="Image" class="image" onclick="window.location.href = 'http://localhost:8080/listing/<%=properties[i]._id%>'">
                    <div class="name"><%= properties[i].title%></div>
                     <div class="price">$<%= properties[i].price%>/night</div>
                </div>
                <% i++; %>
           <% } %> 
            </div>
       <% } %> 
 </div>
</body>
 <script>
    let addbtn = document.querySelector(".add-place");
    addbtn.addEventListener("click" , ()=>{
        window.location.href = 'http://localhost:8080/listing/add';
    })
    let image = document.querySelector(".image");
    image.addEventListener("click" , ()=>{
        // window.location.href = 'http://localhost:8080/listing/<%= %>'

    })
    let home = document.querySelector(".home");
    let all_list = document.querySelector(".all-listing");
    home.addEventListener("click" , ()=>{
        window.location.href = "http://localhost:8080/listing";
    });
     all_list.addEventListener("click" , ()=>{
        window.location.href = "http://localhost:8080/listing";
    });
 </script>
</html>